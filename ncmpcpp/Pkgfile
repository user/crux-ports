# Description: ncmpcpp - featureful ncurses based MPD client inspired by ncmpc
# URL:         http://ncmpcpp.rybczak.net/
# Maintainer:  Ilya Solovyov, shomyoS at gmail.com
# Depends on:  boost curl libmpdclient ncurses readline taglib
# vim:set ts=8 sw=8:

name=ncmpcpp
version=0.8.1
release=1
source=(http://ncmpcpp.rybczak.net/stable/$name-$version.tar.bz2)

build() {
	cd $name-$version

	BOOST_LIB_SUFFIX='' ./configure \
		--prefix=/usr \
		--mandir=/usr/share/man \
		--enable-clock \
		--enable-outputs \
		--enable-unicode \
		--enable-visualizer \
		--with-curl \
		--with-taglib

	make
	make DESTDIR=$PKG install
	install -D -m 0644 -t $PKG/usr/share/ncmpcpp doc/{bindings,config}
	rm -rf $PKG/usr/share/doc
}

